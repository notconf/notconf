module test {
  yang-version 1.1;
  namespace "urn:notconf:test";
  prefix test;

  container bob {
    leaf startup {
      type string;
    }
    leaf bert {
      type string;
    }
    container state {
      config false;
      leaf great {
        type string;
      }
    }
  }
}
